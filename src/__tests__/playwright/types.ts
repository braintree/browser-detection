export interface webCapability {
  browserName: string;
  browserVersion: string;
  osVersion: string;
  os: string;
}

export interface mobileCapability {
  deviceName: string;
  browserName: string;
  osVersion: string;
}
